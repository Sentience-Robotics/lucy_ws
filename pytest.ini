[pytest]
# RoboStack pytest plugins conflict with modern pytest hook specs.
# Entry-point names (importlib.metadata group pytest11), not module paths:
#   launch_testing -> launch_testing.pytest.hooks
#   launch_ros     -> launch_testing_ros_pytest_entrypoint
addopts = -p no:launch_testing -p no:launch_ros
testpaths = tests
