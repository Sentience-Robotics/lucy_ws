# Local overrides for install.sh (copy to `.env`; `.env` is gitignored).

# When true, install.sh uses `url_ssh` from config/repos.json (default: `url_https`).
# Requires SSH keys configured for the host.
DEV=true
