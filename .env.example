# Local overrides for install.sh (copy to `.env`; `.env` is gitignored).

# When true, HTTPS URLs in config/repos.json are rewritten to SSH before git clone
# (supported: github.com, gitlab.com). Requires SSH keys for the host.
DEV=true
